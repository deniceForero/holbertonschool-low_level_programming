/*
 *function isalpha
 *5_print_alpha 0x02-functions_nested_loops
 */
#include "holberton.h"

/**
 * main -is alpha
 * Return: 1
 * Return: 0
 */

int _isalpha(int c)
{
if ((c>= 'a' && c <= 'z') || (c>= 'A' && c <= 'Z')
{
return(1);
else
return(0);
}
    
